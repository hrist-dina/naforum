mixin banner(list)
    section.banner
        .wrapper-inner
            .container
                .banner__list
                    each item in list
                        .banner__item
                            img(src=item, alt="")